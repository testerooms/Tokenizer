{
  "name": "claude-cost-guardian",
  "version": "1.0.0",
  "private": true,
  "description": "Open-source FinOps middleware + dashboard for controlling Claude Code API spend at scale",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=packages/proxy\" \"npm run dev --workspace=packages/dashboard\"",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=packages/proxy && npm run build --workspace=packages/dashboard",
    "start": "npm run start --workspace=packages/proxy",
    "generate-assets": "node scripts/generate-og-image.mjs && node scripts/generate-favicon.mjs",
    "lint": "eslint packages/*/src --ext .ts,.tsx"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "typescript": "^5.3.0",
    "eslint": "^8.56.0"
  }
}
